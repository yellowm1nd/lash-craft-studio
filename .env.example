# ====================================
# Lash Craft Studio - Environment Variables
# ====================================
# IMPORTANT: Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!

# ------------------------------------
# Supabase Configuration
# ------------------------------------
# Get these from your Supabase project settings
# Dashboard: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# DO NOT include VITE_SUPABASE_SERVICE_ROLE_KEY here!
# Service role keys should NEVER be exposed to the client-side

# ------------------------------------
# Admin Configuration
# ------------------------------------
VITE_ADMIN_EMAIL=admin@yourdomain.com

# ------------------------------------
# Development Credentials (DEV ONLY)
# ------------------------------------
# Base64-encoded credentials for local development
# Format: email1:password1,email2:password2
# Only works when import.meta.env.DEV === true
# Example encoding: btoa('admin@example.com:SecurePass123!,test@example.com:TestPass456!')

VITE_DEV_CREDENTIALS=base64-encoded-credentials-here

# ------------------------------------
# Production Deployment Notes
# ------------------------------------
# For Vercel:
# 1. Add these variables in Vercel dashboard (Settings > Environment Variables)
# 2. Ensure .env.local is in .gitignore
# 3. Never commit real credentials to git

# For World4you:
# 1. Add these to your hosting control panel's environment variables
# 2. Ensure proper file permissions (.env files should be 600)
# 3. Use HTTPS only in production

# ------------------------------------
# Security Checklist
# ------------------------------------
# [ ] .env.local is in .gitignore
# [ ] No credentials in source code
# [ ] Service role key NOT in client-side code
# [ ] Production uses strong passwords (12+ chars)
# [ ] Supabase RLS policies enabled
# [ ] Admin users created in Supabase Auth
